# Telugu OCR Project — Python Dependencies
# Install: pip install -r requirements.txt

# ── Core image processing ─────────────────────────────────────────────────────
opencv-python>=4.9.0
Pillow>=10.3.0
scikit-image>=0.22.0
numpy>=1.26.0

# ── PDF handling ──────────────────────────────────────────────────────────────
pdf2image>=1.17.0
pdfplumber>=0.11.0
pypdf>=4.2.0

# ── Deskewing ─────────────────────────────────────────────────────────────────
deskew>=1.5.0
imutils>=0.5.4

# ── Layout analysis ───────────────────────────────────────────────────────────
# layoutparser>=0.3.4       # Optional; requires detectron2 (heavy install)
# doctr[torch]>=0.7.0       # Optional; DocTR layout parser

# ── Baseline OCR ─────────────────────────────────────────────────────────────
pytesseract>=0.3.10
# Tesseract engine + Telugu pack must be installed separately:
#   sudo apt install tesseract-ocr tesseract-ocr-tel

# ── Specialized Indic OCR ────────────────────────────────────────────────────
surya-ocr>=0.4.0
# AI4Bharat IndicOCR — install separately:
#   pip install indicocr  (or from source: https://github.com/AI4Bharat/indicOCR)

# ── LLM APIs ─────────────────────────────────────────────────────────────────
google-generativeai>=0.7.0
openai>=1.30.0
anthropic>=0.28.0

# ── Open-source VLMs (optional, GPU recommended) ─────────────────────────────
# transformers>=4.41.0
# torch>=2.3.0
# accelerate>=0.30.0

# ── OCR evaluation ────────────────────────────────────────────────────────────
jiwer>=3.0.4

# ── Telugu NLP ───────────────────────────────────────────────────────────────
indic-nlp-library>=0.92
# iNLTK — optional:
#   pip install inltk

# ── Data management & visualization ─────────────────────────────────────────
pandas>=2.2.0
matplotlib>=3.9.0
seaborn>=0.13.0
plotly>=5.22.0
tqdm>=4.66.0

# ── Dataset download ─────────────────────────────────────────────────────────
huggingface_hub>=0.23.0
datasets>=2.19.0
